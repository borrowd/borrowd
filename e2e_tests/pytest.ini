[pytest]
testpaths = tests
pythonpath = .
addopts = --alluredir=allure-results --reruns 2 --reruns-delay 5
markers =
    smoke: critical user journeys — run on every deploy
    regression: full test suite — run on every PR
